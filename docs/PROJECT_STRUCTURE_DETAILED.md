# 📁 Структура проекта

Этот документ предоставляет полный обзор структуры и организации проекта BookStore API.

## 🏗️ Корневая структура директорий

```
bookstore-api/
├── 📁 bookstore/              # Основной пакет приложения
├── 📁 tests/                  # Набор тестов
├── 📁 config/                 # Конфигурационные файлы (nginx, prometheus и т.д.)
├── 📁 database/               # SQL файлы и схемы базы данных
├── 📁 docs/                   # Документация
├── 📁 examples/               # Примеры кода и обучающие материалы
├── 📁 scripts/                # Утилиты и скрипты развертывания
├── 📁 k8s/                    # Манифесты Kubernetes
├── 📁 grafana/                # Дашборды мониторинга
├── 📁 .github/                # GitHub workflows и шаблоны
├── 📁 .kiro/                  # Спецификации Kiro
├── 🐳 Docker файлы            # Конфигурация контейнеров
├── ⚙️ Конфигурационные файлы  # Настройки приложения (корневой уровень)
├── 📋 Файлы зависимостей      # Python зависимости
├── 📚 Файлы документации      # Документация проекта
└── 🔧 Инструменты разработки  # Makefile, скрипты и т.д.
```

## 📦 Основное приложение (`bookstore/`)

Главный пакет приложения, содержащий всю бизнес-логику:

```
bookstore/
├── __init__.py                # Инициализация пакета
├── main.py                    # Точка входа FastAPI приложения
├── config.py                  # Управление конфигурацией окружения
├── database.py                # Подключение к БД и управление сессиями
├── models.py                  # SQLAlchemy модели базы данных
├── schemas.py                 # Pydantic схемы запросов/ответов
├── auth.py                    # JWT аутентификация и авторизация
├── logging_config.py          # Конфигурация структурированного логирования
├── middleware.py              # Пользовательские middleware (безопасность, логи, метрики)
└── routers/                   # Обработчики API маршрутов
    ├── __init__.py
    ├── books.py               # Эндпоинты управления книгами
    ├── users.py               # Эндпоинты управления пользователями
    ├── reviews.py             # Эндпоинты системы отзывов
    └── reading_lists.py       # Эндпоинты списков для чтения
```

### Ключевые компоненты

#### `main.py` - Точка входа приложения
- Фабрика FastAPI приложения
- Регистрация middleware
- Подключение роутеров
- Конфигурация CORS
- Обработчики исключений

#### `config.py` - Управление конфигурацией
- Настройки на основе окружения (dev/staging/prod/test)
- Pydantic Settings для валидации
- Конфигурация базы данных
- Настройки безопасности
- Параметры настройки производительности

#### `models.py` - Модели базы данных
- SQLAlchemy ORM модели
- Связи между таблицами
- Индексы и ограничения
- Методы и свойства моделей

#### `schemas.py` - API схемы
- Pydantic модели для валидации запросов
- Схемы сериализации ответов
- Логика трансформации данных
- Интеграция с документацией API

#### `auth.py` - Система аутентификации
- Генерация и валидация JWT токенов
- Хэширование паролей с bcrypt
- Логика аутентификации пользователей
- Декораторы авторизации

## 🧪 Набор тестов (`tests/`)

Комплексная система тестирования с множественными стратегиями:

```
tests/
├── __init__.py
├── conftest.py                # Конфигурация pytest и фикстуры
├── factories.py               # Фабрики тестовых данных с Faker
├── locustfile.py              # Конфигурация нагрузочного тестирования
├── test_unit_basic.py         # Unit тесты основной функциональности
├── test_api_integration.py    # Интеграционные тесты API эндпоинтов
├── test_property_based.py     # Property-based тесты с Hypothesis
└── test_performance.py        # Тесты производительности и нагрузки
```

### Стратегии тестирования

1. **Unit тесты** (`test_unit_basic.py`)
   - Тестирование отдельных функций и классов
   - Мокирование внешних зависимостей
   - Быстрое выполнение, изолированные тесты

2. **Интеграционные тесты** (`test_api_integration.py`)
   - Тестирование полных API workflow
   - Реальные взаимодействия с базой данных
   - Валидация end-to-end функциональности

3. **Property-based тесты** (`test_property_based.py`)
   - Автоматическая генерация тестовых данных
   - Тестирование универсальных свойств
   - Обнаружение граничных случаев

4. **Тесты производительности** (`test_performance.py`)
   - Валидация времени отклика
   - Тестирование пропускной способности
   - Мониторинг использования ресурсов

## 📚 Документация (`docs/`)

Комплексная документация проекта:

```
docs/
├── СТРУКТУРА_ПРОЕКТА.md       # Этот файл
├── PRODUCTION_DEPLOYMENT.md   # Руководство по production развертыванию
├── DOCKER_SETUP.md           # Руководство по конфигурации Docker
├── CI_CD_SETUP.md            # Документация CI/CD пайплайна
├── TESTING_SUMMARY.md        # Обзор системы тестирования
├── DEPLOYMENT_SUMMARY.md     # Сводка вариантов развертывания
└── DEVOPS_PROGRESS.md        # Прогресс внедрения DevOps
```

## 💡 Примеры (`examples/`)

Примеры кода и обучающие материалы:

```
examples/
├── oop_cheatsheet.md          # Руководство по объектно-ориентированному программированию
├── decorators_typehints_cheatsheet.md # Продвинутые возможности Python
├── fastapi_cheatsheet.md      # Руководство по разработке FastAPI
├── testing_cheatsheet.md      # Лучшие практики тестирования
├── oop_practice.py            # Примеры и упражнения по ООП
├── decorators_advanced.py     # Продвинутые паттерны декораторов
├── type_hints_advanced.py     # Примеры типизации
└── task_system.py             # Пример системы управления задачами
```

## 🔧 Скрипты (`scripts/`)

Утилиты для разработки и операций:

```
scripts/
├── setup-dev.sh               # Настройка среды разработки
├── production-health-check.sh # Скрипт мониторинга production
├── backup-script.sh           # Автоматизация резервного копирования БД
├── validate-project.sh        # Скрипт валидации проекта
└── validate-project.ps1       # Windows скрипт валидации проекта
```

## ☸️ Kubernetes (`k8s/`)

Манифесты развертывания Kubernetes:

```
k8s/
├── namespace.yaml             # Namespace Kubernetes
├── configmap.yaml             # Конфигурация приложения
├── secrets.yaml               # Конфиденциальная конфигурация (шаблон)
├── postgresql.yaml            # Развертывание базы данных
├── redis.yaml                 # Развертывание кэша
├── api-deployment.yaml        # Развертывание API приложения
├── ingress.yaml               # Конфигурация балансировщика нагрузки
├── monitoring.yaml            # Prometheus и Grafana
└── deploy.sh                  # Скрипт автоматизации развертывания
```

## 📊 Мониторинг (`grafana/`)

Конфигурация мониторинга и наблюдаемости:

```
grafana/
├── dashboards/
│   ├── dashboard.yml          # Провизионинг дашбордов
│   └── bookstore-api.json     # Основной дашборд приложения
└── datasources/
    └── prometheus.yml         # Конфигурация источников данных
```

## 🚀 CI/CD (`.github/`)

GitHub Actions workflows и шаблоны:

```
.github/
├── workflows/
│   ├── ci.yml                 # Основной CI/CD пайплайн
│   ├── dependencies.yml       # Управление зависимостями
│   └── performance.yml        # Тестирование производительности
└── ISSUE_TEMPLATE/            # Шаблоны issues (при необходимости)
```

## ⚙️ Конфигурационные файлы

### Конфигурация инфраструктуры (`config/`)

Централизованные конфигурационные файлы для компонентов инфраструктуры:

```
config/
├── nginx.conf                 # Конфигурация Nginx для разработки
├── nginx-prod.conf            # Конфигурация Nginx для production
├── prometheus.yml             # Конфигурация сбора метрик
├── loki.yml                   # Конфигурация агрегации логов
├── promtail.yml               # Конфигурация отправки логов
└── redis.conf                 # Конфигурация Redis кэша
```

### Файлы базы данных (`database/`)

SQL файлы и схемы базы данных:

```
database/
├── init.sql                   # Инициализация БД для разработки
└── init-prod.sql              # Инициализация БД для production
```

### Конфигурация приложения (корневой уровень)

Специфичная для приложения конфигурация в корневой директории:

- `.env.example` - Шаблон переменных окружения
- `.env.production` - Шаблон production окружения

## 🐳 Конфигурация контейнеров

Docker-связанные файлы в корневой директории:

- `Dockerfile` - Multi-stage сборка контейнера
- `docker-compose.yml` - Локальный стек разработки
- `docker-compose.prod.yml` - Production стек
- `.dockerignore` - Исключения для Docker сборки

## 📋 Зависимости

Управление Python зависимостями:

- `requirements.txt` - Основные зависимости приложения
- `fastapi_requirements.txt` - Специфичные для FastAPI зависимости
- `testing_requirements.txt` - Зависимости фреймворка тестирования
- `requirements.in` - Исходные файлы зависимостей (для pip-tools)

## 🔧 Инструменты разработки

Инструменты разработки и сборки:

- `Makefile` - Команды разработки и автоматизация
- `pyproject.toml` - Конфигурация Python проекта
- `pytest.ini` - Конфигурация Pytest
- `.flake8` - Конфигурация линтинга кода
- `.gitignore` - Исключения Git

## 📄 Файлы проекта

Метаданные проекта и документация:

- `README.md` - Основная документация проекта
- `LICENSE` - MIT лицензия
- `CHANGELOG.md` - История версий и примечания к релизам
- `CONTRIBUTING.md` - Руководство по участию в разработке
- `FINAL_SUMMARY.md` - Сводка завершения проекта

## 🎯 Принципы проектирования

### 1. **Разделение ответственности**
- Четкое разделение между API, бизнес-логикой и слоями данных
- Модульный дизайн с сфокусированными обязанностями
- Слабая связанность между компонентами

### 2. **Управление конфигурацией**
- Конфигурация на основе окружения
- Безопасное управление секретами
- Валидация значений конфигурации

### 3. **Стратегия тестирования**
- Множественные подходы к тестированию для полного покрытия
- Изоляция и повторяемость тестов
- Интеграция тестирования производительности и безопасности

### 4. **Документация в приоритете**
- Комплексная документация для всех компонентов
- Примеры кода и обучающие материалы
- Руководства по развертыванию и эксплуатации

### 5. **Интеграция DevOps**
- Инфраструктура как код
- Автоматизированное тестирование и развертывание
- Встроенный мониторинг и наблюдаемость

### 6. **Безопасность по дизайну**
- Учет безопасности на каждом уровне
- Валидация и санитизация входных данных
- Безопасные настройки по умолчанию

### 7. **Оптимизация производительности**
- Паттерны async/await для I/O операций
- Стратегии кэширования
- Оптимизация запросов к базе данных
- Мониторинг использования ресурсов

## 🚀 Начало работы

### Для разработчиков
1. **Быстрая настройка**: `./scripts/setup-dev.sh`
2. **Запуск разработки**: `make dev`
3. **Запуск тестов**: `make test`
4. **Просмотр документации**: `make docs`

### Для DevOps
1. **Локальное развертывание**: `make docker-dev`
2. **Production развертывание**: `make deploy-prod`
3. **Kubernetes развертывание**: `make k8s-deploy`
4. **Мониторинг здоровья**: `./scripts/production-health-check.sh`

### Для контрибьюторов
1. **Прочитать руководство по вкладу**: `CONTRIBUTING.md`
2. **Настроить среду разработки**: `./scripts/setup-dev.sh`
3. **Запустить проверки качества кода**: `make lint`
4. **Отправить Pull Request**: Следовать GitHub workflow

## 📈 Соображения масштабируемости

### Горизонтальное масштабирование
- Stateless дизайн приложения
- Пулинг подключений к базе данных
- Redis кэширование для данных сессий
- Конфигурация балансировщика нагрузки

### Вертикальное масштабирование
- Лимиты и запросы ресурсов в Kubernetes
- Настройка производительности базы данных
- Оптимизация памяти и CPU
- Размер пула подключений

### Мониторинг и оповещения
- Сбор метрик Prometheus
- Визуализация Grafana
- Агрегация логов с Loki
- Эндпоинты проверки здоровья

Эта структура обеспечивает прочную основу для production-ready приложения с отличным опытом разработчика, комплексным тестированием и надежными вариантами развертывания.